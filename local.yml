---

- hosts: localhost
  user: root
  vars_files:
    - domainpwd.vault
  roles:
    - { role: networkpackagemanager, when: 'ansible_local.checksum.needs_pkg_run' }
    - { role: init, when: 'ansible_local.checksum.needs_repo_run' }
    - { role: custom, when: 'ansible_local.checksum.needs_repo_run' }
    - { role: domain, when: 'ansible_local.checksum.needs_repo_run' }
